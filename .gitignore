# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Virtual envs (kept OUT of iCloud via UV_PROJECT_ENVIRONMENT; ignore any local ones too)
.venv/
venv/
.uv-envs/

# Env / secrets
.env

# Local OPA binary (installed via `make install-opa`)
/bin/opa

# Generated evaluation output. `make eval` writes into reports/weekend_eval/ (fully
# generated, never committed). The committed, byte-stable sample is reports/eval_summary.md.
reports/weekend_eval/

# OS / editor / local tooling
.DS_Store
.idea/
.vscode/
.claude/
